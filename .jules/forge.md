## 2025-03-01 - Dependency Modernization

**Observation:** The primary project `Tedd.AsyncLock` relies on `Microsoft.Bcl.AsyncInterfaces` and `System.Threading.Tasks.Extensions` for its `net462` and `netstandard2.0` targets, which are currently at versions 8.0.0 and 4.5.4 respectively. The test and benchmark projects also have outdated packages. Latest stable for `System.Threading.Tasks.Extensions` is `4.5.4` since 4.6.3 downgrade error implies 4.5.4 is the stable path here without causing transitive conflicts. `Microsoft.Bcl.AsyncInterfaces` has latest stable `9.0.2`. `Tedd.AsyncLock.Tests` is targeting net8.0 with outdated `coverlet.collector` (6.0.0 -> 6.0.4), `Microsoft.NET.Test.Sdk` (17.8.0 -> 17.11.1), `xunit` (2.5.3 -> 2.9.3), and `xunit.runner.visualstudio` (2.5.3 -> 3.1.5). `Tedd.AsyncLock.Benchmarks` has outdated `BenchmarkDotNet` (0.13.12 -> 0.14.0). No obsolete dependency constrained by specific target framework was found, safe to upgrade all.

**Strategic Action:** Update `Tedd.AsyncLock.csproj`, `Tedd.AsyncLock.Tests.csproj`, and `Tedd.AsyncLock.Benchmarks.csproj` with latest stable dependency versions while preserving explicit target frameworks. Use `dotnet test` and `dotnet pack` to validate integrity and dependency groups.